# Required
version: 2

# Image to use
build:
  image: testing

# Configuration
python:
  version: 3.10 # ERROR: Failing, v3.10 not supported
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
