# Required
version: 2

# Image to use
build:
  image: testing

# Configuration
python:
  version: 3.11 # ERROR: Failing, v3.11 not supported
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
